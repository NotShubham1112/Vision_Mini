# Quick Start

1. Create and activate a virtual environment.
2. Install dependencies with `pip install -r requirements.txt`.
3. Download or place your models into the `models/` directory.
4. Run `python main.py` to start experimenting.
